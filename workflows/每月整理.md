# 每月整理 Workflow

## 目的

把一個月的內容整理成可複用資產、商業方向與下月主題。

## 步驟

1. 讀整月 `log.md` 與 `hot.md`。
2. 整理本月最高 Lion Score 的內容。
3. 合併重複 notes，減少孤島。
4. 產出月度 `wiki/synthesis/`。
5. 更新 `hot.md` 的本月主題。
6. 提名 Skill、Asset、Memory。
7. append `log.md`。

