# Output 回流 Workflow

## 使用時機

產出文章、社群貼文、PPT、圖片提示詞、影片腳本、HTML 看板後使用。

## 流程

1. 將成品放 `outputs/`，素材放 `assets/`。
2. 建立一篇 `wiki/notes/` 紀錄：產出目的、使用資料、核心觀點、可重用段落。
3. 若產出形成新框架，寫入 `wiki/concepts/` 或 `wiki/synthesis/`。
4. 若產出關聯圖片、PPT、影片素材，從 note 連回 `assets/`。
5. 更新 `index.md`、`hot.md`、`log.md`。