# Output 回流 Workflow

## 目的

確保文章、PPT、圖片提示詞、影片腳本、HTML 看板等產出不只是完成任務，而會回流知識庫。

## 步驟

1. 保存產出到 `outputs/`。
2. 連結相關 `wiki/` 條目。
3. 將產出中新增的觀點回寫到 `wiki/synthesis/`。
4. 若產出使用圖片、附件或 PPT 素材，連回 `assets/`。
5. 評估是否形成可複用模板或 Skill。
6. 更新 `hot.md` 的待回流。
7. append `log.md`。

